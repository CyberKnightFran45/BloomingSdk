# CMakeLists.txt root: BloomingSdk

cmake_minimum_required(VERSION 3.8)
project("BloomingSdk")

# Add subprojects

add_subdirectory(BlossomLib)
add_subdirectory(SexyCryptors)
# continue here...